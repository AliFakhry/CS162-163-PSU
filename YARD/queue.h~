#ifndef QUEUE_H
#define QUEUE_H
#include "node.h"
#include <iostream>
using namespace std;

class queue() 
{
 public:
  Node* returnHead();
  Node* returnTail();
  Node* dequeue();
  void enqueue(Node* ADD);
  Node* queueTail;
  Node* queueHead;

};

#endif
