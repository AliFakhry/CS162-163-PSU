#ifndef NODE_H
#define NODE_H 
#include <iostream>
#include <cstring>
#include "Student.h"

using namespace std;

class Node {
 public:
  Node();
  ~Node();
  void setStudent(char* newStudent);
  Student* getStudent();
  void setNext(Node* newNext);
  Node* getNext();
  Node* student;
  Node* next;
};
#endif
